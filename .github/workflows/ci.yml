name: CI Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:

env:
  IMAGE_BACK: docker.io/franperrone/tpi-back
  IMAGE_FRONT: docker.io/franperrone/tpi-front


jobs:
  build-backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: back
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Instalar dependencias
        run: |
          python -m venv venv
          source venv/bin/activate
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Ejecutar tests
        run: |
          source venv/bin/activate
          pytest

  build-frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: front
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Validar estructura del frontend
        run: |
          echo "Archivos en front/"
          ls -la

# CD:
  docker-publish:
    needs: [build-backend, build-frontend]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build backend image
        run: docker build -t $IMAGE_BACK:latest ./back

      #modificacion de prubea para el frontend

      - name: Inyectar URL del backend en index.html
        run: |
          sed -i 's|<script>window.__API_BASE__="https://tpi-back.onrender.com"</script>|<!--__API_BASE__-->|' ./front/index.html

      # <script>window.__API_BASE__="https://tpi-back.onrender.com"</script>
      # <!--__API_BASE__-->


      - name: Build frontend image
        run: docker build -t $IMAGE_FRONT:latest ./front

      - name: Push backend image
        run: docker push $IMAGE_BACK:latest

      - name: Push frontend image
        run: docker push $IMAGE_FRONT:latest